extends ../layout

block content
	.container
		.row
			.col-md-7
				-/*flash*/
				embed(src = "#{commodity.flash}", allowFullScreen = "true", quality = "high", width = "720", height = "600", align = "middle", allowScriptAcess="always", type="application/x-shockwave-flash")
			.col-md-5
				dl.dl-horizontal
					dt Name
					dd= commodity.title
					dt  orign
					dd= commodity.orign
					dt Year
					dd= commodity.year
					dt Summary
					dd= commodity.summary
					.btn-pay
						a.btn.btn-primary(href = "", role= "button") Pay for it